[pytest]
# Permite "from conftest import ..." en cada suite sin duplicar código,
# y evita colisiones de nombres de archivos de test entre carpetas.
addopts = -ra --import-mode=importlib
testpaths =
    api
    smoke
    regression
